# Known Limitations

See [Solana: Limitations](../solana/limitations.md) for the backend-specific list: no static CFG (so no `slice` / structural `trace` yet), heuristic `who`, `time-warp` semantics, keypair persistence model, CPI visibility.

For the planned remediations, see the [Roadmap](../roadmap/solana.md).
